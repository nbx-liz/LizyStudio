{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment_purpose": "Issue #539: mutation testing for useConfigWriteFunnel. Run via `pnpm stryker run` or the nightly CI job (.github/workflows/nightly.yml::mutation-test). Mutation score is a trend metric, not a PR gate. Tightening per module is a follow-up.",
  "packageManager": "pnpm",
  "testRunner": "vitest",
  "vitest": {
    "configFile": "vitest.config.ts"
  },
  "_comment_scope": "Scope is intentionally narrow — only the funnel pure functions where the v0.6.2 / #530 / #533 bug class lived. Widening the set without first acting on the current report is wasted CI time.",
  "mutate": [
    "src/hooks/useConfigWriteFunnel.ts"
  ],
  "_comment_thresholds": "Thresholds match the pyproject.toml mutmut posture: report-only, never gate on score. The high water mark is a metric exported via the nightly artefact.",
  "thresholds": {
    "high": 100,
    "low": 0,
    "break": 0
  },
  "reporters": [
    "progress",
    "clear-text",
    "html"
  ],
  "htmlReporter": {
    "fileName": "stryker-report.html"
  },
  "concurrency": 2,
  "timeoutMS": 30000
}
