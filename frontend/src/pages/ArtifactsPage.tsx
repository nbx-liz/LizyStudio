import { Title, Text, Stack } from "@mantine/core";

export function ArtifactsPage() {
  return (
    <Stack>
      <Title order={2}>Artifacts</Title>
      <Text c="dimmed">Export and load model artifacts.</Text>
    </Stack>
  );
}
